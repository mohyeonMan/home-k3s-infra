apiVersion: v1
kind: Service
metadata:
  name: srrrg-dev-app
  labels:
    app: srrrg-dev-app
spec:
  type: ClusterIP
  selector:
    app: srrrg-dev-app
  ports:
  - name: app
    port: 80
    targetPort: 8080
