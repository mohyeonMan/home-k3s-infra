apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: c2c-ws-ingress
  namespace: c2c
spec:
  ingressClassName: traefik
  rules:
  - host: jhhomehub.gonetis.com
    http:
      paths:
      - path: /ws
        pathType: Prefix
        backend:
          service:
            name: c2c-ws
            port:
              number: 8080
