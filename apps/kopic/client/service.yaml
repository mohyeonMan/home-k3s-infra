apiVersion: v1
kind: Service
metadata:
  name: kopic-client
  namespace: kopic
  labels:
    app: kopic-client
spec:
  type: ClusterIP
  selector:
    app: kopic-client
  ports:
  - name: http
    port: 80
    targetPort: 80
