apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kopic-dev-ge
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: kopic-dev-ge
  namespaceSelector:
    matchNames:
      - kopic
  endpoints:
    - port: app
      path: /actuator/prometheus
      interval: 30s
